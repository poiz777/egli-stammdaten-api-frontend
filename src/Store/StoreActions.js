export default {
  /*
  performAction(context, extraParameter) {
    // `context` HERE IS NO OTHER THAN THE `$store`
    if(context.state.totalTvCount >= amount) {
      context.commit('removeTv', amount)
    }
  }
  */
};
